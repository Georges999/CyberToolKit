<UserControl x:Class="CyberToolKit.PacketSnifferView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Network Interface:" Foreground="White" VerticalAlignment="Center"/>
            <ComboBox x:Name="InterfaceComboBox" Width="300" Margin="5"/>
            <Button x:Name="StartButton" Content="Start Scan" Margin="5" Padding="10,5" Click="StartButton_Click"/>
            <Button x:Name="StopButton" Content="Stop Scan" Margin="5" Padding="10,5" Click="StopButton_Click" IsEnabled="False"/>
            <Button x:Name="RefreshButton" Content="Refresh Interfaces" Margin="5" Padding="10,5" Click="RefreshButton_Click"/>
        </StackPanel>

        <ListView x:Name="PacketListView" Grid.Row="1" Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Timestamp" Width="150" DisplayMemberBinding="{Binding Timestamp}"/>
                    <GridViewColumn Header="Source IP" Width="150" DisplayMemberBinding="{Binding SourceIp}"/>
                    <GridViewColumn Header="Destination IP" Width="150" DisplayMemberBinding="{Binding DestinationIp}"/>
                    <GridViewColumn Header="Protocol" Width="100" DisplayMemberBinding="{Binding Protocol}"/>
                    <GridViewColumn Header="Length" Width="80" DisplayMemberBinding="{Binding Length}"/>
                    <GridViewColumn Header="Info" Width="350" DisplayMemberBinding="{Binding Info}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock x:Name="StatusText" Grid.Row="2" Text="Ready" Foreground="LightGreen" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
